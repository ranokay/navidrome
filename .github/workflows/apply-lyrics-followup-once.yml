name: Apply lyrics lifecycle follow-up

on:
  pull_request:
    branches:
      - master
    paths:
      - .github/workflows/apply-lyrics-followup-once.yml

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: lyrics-sidebar-performance
          fetch-depth: 0
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: ui/package-lock.json
      - name: Apply follow-up fixes
        run: python3 .github/scripts/apply-lyrics-followup.py
      - name: Install dependencies
        working-directory: ui
        run: npm ci
      - name: Format changed files
        working-directory: ui
        run: npx prettier --write src/audioplayer/LyricsPanel.jsx src/audioplayer/LyricsLineRows.jsx src/audioplayer/lyrics.js src/audioplayer/lyricsTimeline.js src/audioplayer/LyricsPanel.test.jsx src/audioplayer/lyricsTimeline.test.js
      - name: Validate UI
        working-directory: ui
        run: |
          npm run check-formatting
          npm run lint
          npm test
          npm run build
      - name: Commit fixes and remove temporary files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          rm -f .github/workflows/apply-lyrics-followup-once.yml
          rm -f .github/scripts/apply-lyrics-followup.py
          git add -A
          git commit -m "fix(ui): stabilize pronunciation and line release timing"
          git push origin HEAD:lyrics-sidebar-performance
