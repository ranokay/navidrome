name: Connect character wave and stabilize spacing

on:
  pull_request:
    branches:
      - master
    paths:
      - .github/workflows/connect-character-wave-and-stabilize-spacing-once.yml

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: lyrics-sidebar-performance
          fetch-depth: 0
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: ui/package-lock.json
      - name: Apply connected wave fix
        run: python3 .github/scripts/connect-character-wave-and-stabilize-spacing.py
      - name: Install dependencies
        working-directory: ui
        run: npm ci
      - name: Format changed files
        working-directory: ui
        run: npx prettier --write src/audioplayer/lyricsKaraokeConstants.js src/audioplayer/useLyricsTimeline.js src/audioplayer/LyricsPanel.jsx src/audioplayer/LyricsPanel.test.jsx src/audioplayer/useLyricsTimeline.test.js
      - name: Validate UI
        working-directory: ui
        run: |
          set +e
          {
            npm run check-formatting
            npm run lint
            npm test
            npm run build
          } > /tmp/connected-wave-validation.log 2>&1
          status=$?
          cat /tmp/connected-wave-validation.log
          if [ "$status" -ne 0 ]; then
            cp /tmp/connected-wave-validation.log ../.github/connected-wave-validation-error.txt
            cd ..
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add .github/connected-wave-validation-error.txt
            git commit -m "chore: capture connected wave validation failure"
            git push origin HEAD:lyrics-sidebar-performance
            exit 1
          fi
      - name: Commit fixes and remove temporary files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          rm -f .github/workflows/connect-character-wave-and-stabilize-spacing-once.yml
          rm -f .github/scripts/connect-character-wave-and-stabilize-spacing.py
          rm -f .github/connected-wave-validation-error.txt
          git add -A
          git commit -m "fix(ui): connect character wave and stabilize glyph spacing"
          git push origin HEAD:lyrics-sidebar-performance
