name: Fix shared lyric opacity override

on:
  pull_request:
    branches:
      - master
    paths:
      - .github/workflows/fix-shared-layer-opacity-override-once.yml

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: lyrics-sidebar-performance
          fetch-depth: 0
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: ui/package-lock.json
      - name: Apply opacity override fix
        run: python3 .github/scripts/fix-shared-layer-opacity-override.py
      - name: Install dependencies
        working-directory: ui
        run: npm ci
      - name: Format changed files
        working-directory: ui
        run: npx prettier --write src/audioplayer/LyricsLineRows.jsx src/audioplayer/LyricsPanel.test.jsx
      - name: Validate UI
        working-directory: ui
        run: |
          npm run check-formatting
          npm run lint
          npm test
          npm run build
      - name: Commit fixes and remove temporary files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          rm -f .github/workflows/fix-shared-layer-opacity-override-once.yml
          rm -f .github/scripts/fix-shared-layer-opacity-override.py
          git add -A
          git commit -m "fix(ui): let shared lyric opacity control idle layers"
          git push origin HEAD:lyrics-sidebar-performance
