name: Apply lyrics phase 3 correctness

on:
  pull_request:
    branches:
      - master
    paths:
      - .github/workflows/apply-lyrics-phase3-correctness-once.yml

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: lyrics-sidebar-performance
          fetch-depth: 0
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: ui/package-lock.json
      - name: Apply timing correctness fixes
        run: python3 .github/scripts/apply-lyrics-phase3-correctness.py
      - name: Install dependencies
        working-directory: ui
        run: npm ci
      - name: Format changed files
        working-directory: ui
        run: npx prettier --write src/audioplayer/lyrics.js src/audioplayer/lyricsTimeline.js src/audioplayer/LyricsPanel.jsx src/audioplayer/lyrics.test.js src/audioplayer/lyricsTimeline.test.js src/audioplayer/LyricsPanel.test.jsx
      - name: Validate UI
        working-directory: ui
        run: |
          npm run check-formatting
          npm run lint
          npm test
          npm run build
      - name: Commit fixes and remove temporary files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          rm -f .github/workflows/apply-lyrics-phase3-correctness-once.yml
          rm -f .github/scripts/apply-lyrics-phase3-correctness.py
          git add -A
          git commit -m "fix(ui): harden lyrics timeline edge cases"
          git push origin HEAD:lyrics-sidebar-performance
