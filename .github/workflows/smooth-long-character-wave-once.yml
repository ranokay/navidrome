name: Smooth long character wave

on:
  pull_request:
    branches:
      - master
    paths:
      - .github/workflows/smooth-long-character-wave-once.yml

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: lyrics-sidebar-performance
          fetch-depth: 0
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: ui/package-lock.json
      - name: Apply smooth wave fix
        run: python3 .github/scripts/smooth-long-character-wave.py
      - name: Install dependencies
        working-directory: ui
        run: npm ci
      - name: Format changed files
        working-directory: ui
        run: npx prettier --write src/audioplayer/lyricsKaraokeConstants.js src/audioplayer/useLyricsTimeline.js src/audioplayer/LyricsPanel.jsx src/audioplayer/LyricsPanel.test.jsx src/audioplayer/useLyricsTimeline.test.js
      - name: Validate UI
        working-directory: ui
        run: |
          set +e
          {
            npm run check-formatting
            npm run lint
            npm test
            npm run build
          } > /tmp/smooth-wave-validation.log 2>&1
          status=$?
          cat /tmp/smooth-wave-validation.log
          if [ "$status" -ne 0 ]; then
            cp /tmp/smooth-wave-validation.log ../.github/smooth-wave-validation-error.txt
            cd ..
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add .github/smooth-wave-validation-error.txt
            git commit -m "chore: capture smooth wave validation failure"
            git push origin HEAD:lyrics-sidebar-performance
            exit 1
          fi
      - name: Commit fixes and remove temporary files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          rm -f .github/workflows/smooth-long-character-wave-once.yml
          rm -f .github/scripts/smooth-long-character-wave.py
          rm -f .github/smooth-wave-validation-error.txt
          git add -A
          git commit -m "fix(ui): smooth long-duration character waves"
          git push origin HEAD:lyrics-sidebar-performance
